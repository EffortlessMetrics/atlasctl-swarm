# Spec Style

The durable repo knowledge base lives in `.atlasctl-swarm-spec/`.

Use separate artifacts for:
- why (proposal)
- what (spec)
- decision (ADR)
- how (implementation plan + lane tracker)
- what proves it (evidence / CI proof)
- what happened (closeout)
