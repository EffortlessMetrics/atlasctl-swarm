schema_version = "1.0"

id = "<policy-ref-id>"
name = "<Policy ledger name>"
status = "active"
owner = "<owner>"

ledger_path = "<path-to-live-ledger>"
notes = "Reference to live policy enforcement source; do not duplicate ledger content here."
